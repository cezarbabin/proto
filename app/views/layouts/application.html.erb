<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) %></title>
  <%= stylesheet_link_tag 'application', media: 'all',
                          'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,700' rel='stylesheet' type='text/css'>  <!--[if lt IE 9]>
  <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/r29/html5.min.js">
  </script>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <![endif]-->
</head>
<body>

  <div class="navbar">


    <h1> pentalist </h1>
    <div class="container" id="header"">

      <% if logged_in? %>

        <%= link_to "Submit", '#' %>
           <%= link_to "Profile", edit_profile_path(current_user), style:'padding-left:20px' %>
           <%= link_to "Your list", new_friend_path, style:'padding-left:20px' %>
      <% else %>
         <%= link_to "Sign Up", signup_path %>
        <%= link_to "Log in", login_path, style:'padding-left:20px'%>
      <% end %>
      <hr>

    </div>

  </div>
  <% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %>"><%= message %></div>
<% end %>
  <%= yield %>

    <div class="container" id="footer" >
      <hr>
      <% if logged_in? %>
        <%= link_to "Log out", logout_path, method: :delete %>
        <%= link_to "Settings", edit_user_path(current_user),
                    style:'padding-left:20px;
                  ' %>
      <% else %>
          <footer>
            <small>© Copyright 2016,  <%= link_to 'Pentalist LLC', "#"%></small>
          </footer>
      <% end %>

    </div>

</body>
</html>